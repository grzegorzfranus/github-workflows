---
name: "Ansible Security"

on:
  workflow_call:
    inputs:
      enable-trufflehog:
        description: "Enable TruffleHog secret scanning"
        type: boolean
        default: true
      enable-trivy:
        description: "Enable Trivy IaC security scans"
        type: boolean
        default: true
      runner-group:
        description: "Runner group to execute jobs on. Ignored when 'runner' is set."
        type: string
        default: ""
      runner:
        description: "Explicit runner label. Overrides 'runner-group' when non-empty."
        type: string
        default: "ubuntu-latest"

permissions:
  contents: read

jobs:
  # ================================================================
  # 1. SECRET DETECTION (TruffleHog)
  # ================================================================
  secret-scan:
    name: "🔒 Scan for Secrets"
    if: ${{ inputs.enable-trufflehog }}
    runs-on: ${{ inputs.runner != '' && inputs.runner || fromJSON(format('{{"group":"{0}"}}', inputs.runner-group)) }}
    timeout-minutes: 10
    steps:
      - name: "🔍 Checkout repository"
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: "🔒 Run TruffleHog scanner"
        uses: trufflesecurity/trufflehog@bcfcf73aaf4759d4dadc2783177c245a02792318 # v3.97.0
        with:
          version: "3.97.0"

  # ================================================================
  # 2. SECURITY IaC SCAN (Trivy)
  # ================================================================
  iac-scan:
    name: "🔒 Scan IaC Misconfigurations"
    if: ${{ inputs.enable-trivy }}
    runs-on: ${{ inputs.runner != '' && inputs.runner || fromJSON(format('{{"group":"{0}"}}', inputs.runner-group)) }}
    timeout-minutes: 10
    steps:
      - name: "🔍 Checkout repository"
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          persist-credentials: false

      - name: "⚙️ Create dummy .trivyignore if missing"
        run: |
          if [ ! -f .trivyignore ]; then
            echo "# Empty trivyignore file" > .trivyignore
          fi

      - name: "🔒 Run Trivy IaC scan"
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          version: "v0.73.0"
          scan-type: "fs"
          scan-ref: "."
          scanners: "misconfig"
          severity: "HIGH,CRITICAL"
          exit-code: "1"
          format: "table"
          trivyignores: ".trivyignore"
        env:
          TRIVY_MISCONFIG_SCANNERS: "ansible"

  # ================================================================
  # 3. SECURITY GATE
  # ================================================================
  security-gate:
    name: "✅ Security Gate"
    needs: [secret-scan, iac-scan]
    if: always()
    runs-on: ${{ inputs.runner != '' && inputs.runner || fromJSON(format('{{"group":"{0}"}}', inputs.runner-group)) }}
    timeout-minutes: 5
    steps:
      - name: "⚙️ Evaluate results"
        env:
          SECRET_SCAN_RESULT: ${{ needs.secret-scan.result }}
          IAC_SCAN_RESULT: ${{ needs.iac-scan.result }}
        run: |
          echo "Secret Scan (TruffleHog): $SECRET_SCAN_RESULT"
          echo "IaC Scan (Trivy):         $IAC_SCAN_RESULT"

          has_failures=0

          if [[ "$SECRET_SCAN_RESULT" == "failure" || "$SECRET_SCAN_RESULT" == "cancelled" ]]; then
            echo "::error::TruffleHog secret scanning failed or was cancelled"
            has_failures=1
          fi

          if [[ "$IAC_SCAN_RESULT" == "failure" || "$IAC_SCAN_RESULT" == "cancelled" ]]; then
            echo "::error::Trivy IaC scanning failed or was cancelled"
            has_failures=1
          fi

          if [[ "$has_failures" -ne 0 ]]; then
            exit 1
          fi

          echo "All security scans passed successfully"
